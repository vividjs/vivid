export let syntax = {
	openingOutput: '{{',
	closingOutput: '}}',
	openingStatement: '{%',
	closingStatement: '%}',
	openingBlockHelper: '{#',
	closingBlockHelper: '#}',
};


// import {getQueryParams} from './utils';

// const params = getQueryParams();
// const JTML_IN_URL = !!params.jtml;

// export const IS_DEV_MODE = JTML_IN_URL && params.jtml.includes('dev');
// export const VERBOSE_MODE = JTML_IN_URL && params.jtml.includes('verbose');

// console.log('DEV MODE:', IS_DEV_MODE);
// console.log('VERBOSE MODE:', VERBOSE_MODE);
